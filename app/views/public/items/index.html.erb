<h2>商品一覧 <span>(全<%= @item.count %>件)</span></h2>

<% @items.each do |item| %>
<%= image_tag item.get_image %>
<p><%= item.name %></p>
<p>¥<%= item.with_tax_price.to_s(:delimited) %></p>
<% end %>

<%= paginate @items %>